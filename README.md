# ASCIIArtGenerator
converts image to text representation

out put looks the best in a text editor where u turn the font size to 1 and also light mode for rigth brightness alternativ is a negativ image in darkmode
default characters used for shading are "@", "#", "S", "%", "?", "*", "+", ";", ":", ",", " "